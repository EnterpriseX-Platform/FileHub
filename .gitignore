# Rust
backend/target/

# Node / Next.js
frontend/node_modules/
frontend/.next/
frontend/out/

# Env files (keep the .example templates)
.env
.env.*
!.env.example

# Local data / uploads
storage/
backend/storage/
backend/filehub.db*

# OS / logs
.DS_Store
*.log

# Stray root ./storage when the backend is run from the repo root, local env files,
# and the per-machine agent settings — none belong in version control.
/storage/
.env*.local
.claude/settings.local.json
.claude/

# TypeScript incremental build cache (generated by tsc)
*.tsbuildinfo
